{
  "name": "adaptiq",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Self-improving AI runtime",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r --parallel dev",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.4.0",
    "tsup": "^8.0.0",
    "prettier": "^3.2.0",
    "dotenv": "^16.0.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=8"
  }
}